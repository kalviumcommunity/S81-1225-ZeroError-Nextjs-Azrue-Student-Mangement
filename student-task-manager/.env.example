## Template for required environment variables
## Copy this file to `.env.local` for local development and fill in real values.
## Only variables starting with `NEXT_PUBLIC_` are exposed to the client.

# Client-side variables (safe to expose)
# Base URL for your public API used by the browser. Example points to local Next.js API route.
NEXT_PUBLIC_API_URL=http://localhost:3000/api
# Optional label used in logs/UX to indicate environment on client.
NEXT_PUBLIC_APP_ENV=development

# Server-only variables (NEVER used in Client Components/Hooks)
# PostgreSQL connection string used by server-side code only.
DATABASE_URL=mongodb+srv://root:root@cluster0.aca27.mongodb.net/student_mangement?retryWrites=true&w=majority&appName=Cluster0

# Redis connection URL for caching layer. Use local Redis or Redis Cloud.
REDIS_URL=redis://localhost:6379

# Secret used for session/auth encryption. Use a strong random string (32+ chars).
AUTH_SECRET=ertdfygjuhfsdghj
# Secret used for signing JWT tokens. Use a strong random string (32+ chars).
JWT_SECRET=ertgfhjghgfasa23456ty

# ------------------------------------------------------------
# Cloud Storage (choose ONE option: AWS S3 or Azure Blob)
# ------------------------------------------------------------

# Option A: AWS S3 (server-only)
# Install: npm install @aws-sdk/client-s3 @aws-sdk/s3-request-presigner
# Docs: https://docs.aws.amazon.com/AmazonS3/latest/userguide/ShareObjectPreSignedURL.html
# Required for presigned URL generation in server routes.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
AWS_BUCKET_NAME=

# Option B: Azure Blob Storage (server-only)
# Install: npm install @azure/storage-blob
# Docs: https://learn.microsoft.com/azure/storage/blobs/storage-blobs-overview
AZURE_STORAGE_ACCOUNT_NAME=
AZURE_STORAGE_ACCOUNT_KEY=
AZURE_CONTAINER_NAME=

# SendGrid (Transactional Email)
# Create API key in SendGrid and verify sender email.
SENDGRID_API_KEY=replace-me-sendgrid-api-key
SENDGRID_SENDER=no-reply@example.com

# Staging reference (copy into .env.staging, do not commit real values)
# NEXT_PUBLIC_API_URL=https://staging.api.studentapp.com
# NEXT_PUBLIC_APP_ENV=staging
# DATABASE_URL=postgres://staging_user:staging_pass@staging-db:5432/studentdb_staging
# AUTH_SECRET=replace-me-staging
# JWT_SECRET=replace-me-staging-jwt

# Production reference (copy into .env.production)
# NEXT_PUBLIC_API_URL=https://api.studentapp.com
# NEXT_PUBLIC_APP_ENV=production
# DATABASE_URL=postgres://prod_user:prod_pass@prod-db:5432/studentdb
# AUTH_SECRET=replace-me-prod
# JWT_SECRET=replace-me-prod-jwt
# SENDGRID_API_KEY=prod-sendgrid-api-key
# SENDGRID_SENDER=no-reply@yourdomain.com
